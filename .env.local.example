# Copy this file to .env.local and set your access code.
# The app will show a passcode gate to all visitors until VITE_ACCESS_CODE is removed.
# To go public: delete this variable from Vercel environment settings — no code changes needed.

VITE_ACCESS_CODE=your-secret-code-here
